import * as settings from '@store/types/settings'
import * as app from '@store/types/app'
import * as profile from '@store/types/profile'
import * as friends from '@store/types/friends'

export { settings, app, profile, friends }
