import { height, sp } from '@utils'

export default {
  width: height(300),
  height: height(300),
  outerMaskOpacity: 0.5,
  edgeBorderWidth: sp(2),
  edgeWidth: '100%',
  edgeHeight: '100%',
  showAnimatedLine: false,
}
